name: PR-Test
on: pull_request

jobs:
  example_matrix:
    name: PRBuild
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-2022.04, ubuntu-2024.04]
    steps:
      - run: echo "Hello !"
